funtion kertotaulu() {
	let luku = parseInt(document.getElementById("luku").value);
	let lause = "";
	for (var i= 1; i <=1+; i++) {
		lause += "luku " +luku + "*" + "=" + luku * i + "\n";
	}
	console.log(lause);
}